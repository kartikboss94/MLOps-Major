apiVersion: v1
kind: Service

metadata:
  name: olivetti-face-service

spec:
  type: NodePort

  selector:
    app: olivetti-face-app

  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30007